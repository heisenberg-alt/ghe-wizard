run:
  timeout: 5m

linters:
  disable-all: true
  enable:
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - unconvert

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
